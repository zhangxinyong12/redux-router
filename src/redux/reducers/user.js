import * as actionType from '../action-types';
const init={
    userInfo:{},
    error:''
};
export default (state=init,action)=>{
    switch(action.type){
        
        default :
            return state;
    }
}